import React from 'react';

function Lobby() {
    return (
        <div>
            <input type="text" name="name"/>
            <button>START</button>
        </div>
    );
}

export default Lobby;